apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - name: backend
      protocol: TCP
      port: 3000
      targetPort: 3000

  # Note here that we match both the app to balance the traffic
  selector:
    app: backend